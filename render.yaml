services:
  - type: web
    name: pdf-processor
    env: node
    buildCommand: apt-get update && apt-get install -y qpdf && npm install && cd client && npm install && npm run build && cd ..
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 18.x