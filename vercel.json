{
  "version": 2,
  "buildCommand": "npm install && cd client && npm install && npm run build",
  "builds": [
    {
      "src": "api.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb",
        "includeFiles": "client/build/**"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api.js"
    }
  ]
}
